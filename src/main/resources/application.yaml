spring:
  profiles:
    active: dev
  rabbitmq:
    username: admin
    password: admin
  liquibase:
    enabled: true
    change-log: classpath:/config/liquibase/changelog.xml

server:
  port: 8081

cinema:
  max-session-hours: 5
  min-session-minutes: 10
